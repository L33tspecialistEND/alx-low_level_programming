#include <stdio.h>
/**
 * main - Prints the alphabet
 *
 * Return: 0
 */

int main(void)
{

	int alphabet = 'a';
	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(alphabet);
	}

	return (0);
}
