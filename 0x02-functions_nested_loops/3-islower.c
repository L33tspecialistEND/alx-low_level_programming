#include "main.h"

/**
 * _islower - Checks if a character is lowercase
 *
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 97 && c<= 12)
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
