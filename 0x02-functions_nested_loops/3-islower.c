#include "main.h"

/**
 * _islower - Checks if a character is lowercase
 * @c: Number to pass _islower function
 */
void _islower(int c)
{
	if (c >= 97 && c<= 12)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
