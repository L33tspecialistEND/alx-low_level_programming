#include "main.h"

/**
 * _isdigit - Checks for a number between 0 and 9
 * @c: Number to pass to the function
 *
 * Return: 1 or 0
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
