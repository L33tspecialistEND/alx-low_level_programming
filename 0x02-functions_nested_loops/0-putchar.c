#include "main.h"

/**
 * main - Prints '_putchar'
 *
 * Return: 0
 */
int main(void)
{
	char str[] = {_, p, u, t, c, h, a, r};
	int count;

	for(count = 0; count <= 7; count++)
	{
		_putchar(str[count]);
	}
	_putchar('\n');

	return (0);
}
