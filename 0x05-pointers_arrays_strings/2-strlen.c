#include "main.h"

/**
 * _strlen - Returns the length of a string
 * @s: Variable to store the string
 * Return: len
 */
int _strlen(char *s)
{
	int len;
	char str[1000];
	
	s = &str;
	len = sizeof (str);

	return (len);
}
